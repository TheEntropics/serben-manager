<h1>Available games</h1>

<div class="row">
  <div class="col-md-6">
    <div class="list-group">
      <% @games.each do |game| %>
        <%= render 'game', game: game %>
      <% end %>
    </div>
  </div>
</div>
